services:
  # Backend API Service
  - type: web
    name: eduflow-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./lms.db
      - key: ENVIRONMENT
        value: production
      - key: CORS_ORIGINS
        value: https://eduflow-frontend.onrender.com,http://localhost:5173
      - key: GEMINI_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: JWT_SECRET_KEY
        value: demo-jwt-secret-key-for-render-deployment
    healthCheckPath: /api/health

  # Frontend Static Site Service
  - type: web
    name: eduflow-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    rootDir: frontend
    envVars:
      - key: VITE_API_URL
        value: https://eduflow-backend.onrender.com
      - key: VITE_NODE_ENV
        value: production
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache, no-store, must-revalidate
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
